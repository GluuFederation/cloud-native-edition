apiVersion: v1
kind: PersistentVolume
metadata:
  name: config-pv
  labels:
      config-init: config
spec:
  capacity:
    storage: STORAGECONFIG
  accessModes:
    - ReadWriteOnce
  awsElasticBlockStore:
    volumeID: CONFIGVOLUMEID
    fsType: ext4