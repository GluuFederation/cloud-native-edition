kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: casa-pvc
spec:
  storageClassName: ""
  volumeName: casa-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: STORAGECASA
  selector:
    matchLabels:
      type: casa