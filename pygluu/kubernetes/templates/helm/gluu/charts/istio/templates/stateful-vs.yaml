apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ include "istio.name" .}}-stateful
  namespace: {{ .Release.Namespace }}
spec:
  gateways:
  - {{ .Release.Name }}-global-gtw 
  hosts:
  - {{ .Values.global.domain }}
  http:
  - name: oxtrust-route
    match:
    - uri:
        exact: /identity
    route:
    - destination:
        host: oxtrust.{{ .Release.Namespace }}.svc.cluster.local
        port:
          number: 8080
      weight: 100
  - name: shibboleth-route
    match:
    - uri:
        prefix: /idp
    rewrite:
      uri: /identity
    route:
    - destination:
        host: oxshibboleth.{{ .Release.Namespace }}.svc.cluster.local
        port:
          number: 8080
      weight: 100
  - name: passport-route
    match:
    - uri:
        prefix: /passport
    rewrite:
      uri: /identity
    route:
    - destination:
        host: oxpassport.{{ .Release.Namespace }}.svc.cluster.local
        port:
          number: 8090
      weight: 100