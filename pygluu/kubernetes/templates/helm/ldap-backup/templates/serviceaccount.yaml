{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ldap-backup.serviceAccountName" . }}
  labels:
{{ include "ldap-backup.labels" . | nindent 4 }}
{{- end -}}
