{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "couchbase-backup.serviceAccountName" . }}
  labels:
{{ include "couchbase-backup.labels" . | nindent 4 }}
{{- end -}}
